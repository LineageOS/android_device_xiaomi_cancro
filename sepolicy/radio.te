allow radio rmnet_device:chr_file rw_file_perms;

# Grant access to qmux socket that is created by rild
allow radio qmuxd_socket:sock_file rw_file_perms;

allow radio rild_qmuxd_socket:sock_file write;
allow radio unlabeled:dir search;
allow radio unlabeled:file { read getattr open };
